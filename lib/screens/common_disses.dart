import 'package:flutter/material.dart';

class CommonDisses extends StatelessWidget {
  const CommonDisses({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Common Disses");
  }
}
